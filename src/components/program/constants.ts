// ── Toast notification status codes ────────────────────────────────────────
export enum ToastStats {
    Delete = 'delete',
    Success = 'success',
    Error = 'error',
    Loading = 'loading',
    Info = 'info',
    Warning = 'warning',
}
